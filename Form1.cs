///// Forms1 handles the US shell, tray behavior, and wires the application together /////

// Namespaces
using System;
using System.Windows.Forms;
using ScreenGrab.Services; // Functionalities are split into seperate services
namespace ScreenGrab
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }
    }
}
